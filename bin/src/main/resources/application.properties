spring.application.name=financetracker
server.port=8080

# Pulled from the .env file at project root via spring-dotenv.
# Never hardcode the real secret here.
jwt.secret=${JWT_SECRET}
jwt.expiration-ms=${JWT_EXPIRATION_MS:3600000}
